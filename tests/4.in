int max(x, y) {
    if (x>y) return x;
    return y;
}

int b(x) {
    return 2;
}

int a(x, y, w) {
    z=b(x);
    return (z+x)*max(y, w);
}

int gcd(a, b) {
    if (a == 0) return b;
    return gcd(b % a, a);
}

return gcd(12, 8) + gcd(21, 7) + gcd(30, 42) + a(4, 6, 8);
